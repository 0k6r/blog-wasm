{
  "private": true,
  "scripts": {
    "install": "cd frontend && yarn install",
    "build": "cd frontend && yarn run build",
    "start": "cd frontend && yarn start",
    "test": "cd frontend && yarn run test"
  }
}
